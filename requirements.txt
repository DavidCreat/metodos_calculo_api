# requirements.txt para la API Flask del solucionador
flask==2.3.3
flask-cors==3.0.10
numpy==1.26.4
sympy==1.12
# Si usas requests para consumo externo (opcional)
requests==2.31.0
# Si usas pandas para reportes o manipulación de datos (opcional)
pandas==2.2.2
